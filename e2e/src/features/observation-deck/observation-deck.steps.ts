import { Given, When, Then } from 'cucumber';

Given(/^the latest observation$/, async () => {
});

When(/^I react positively to the observation$/, async () => {
});

Then(/^I should be notified that the system acknowledged my reaction$/, async () => {
});
